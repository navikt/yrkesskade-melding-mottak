query($journalpostId: String!) {
  journalpost(journalpostId: $journalpostId) {
    journalpostId
    journalstatus
    journalposttype
    tema
    dokumenter {
      tittel
      brevkode
    }
    bruker {
      id
      type
    }
    datoOpprettet
  }
}
