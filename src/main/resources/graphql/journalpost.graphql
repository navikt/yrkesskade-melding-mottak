query($journalpostId: String!) {
  journalpost(journalpostId: $journalpostId) {
    journalpostId
    journalstatus
    behandlingstema
    tema
    bruker {
      id
      type
    }
    sak {
      fagsakId
    }
  }
}
